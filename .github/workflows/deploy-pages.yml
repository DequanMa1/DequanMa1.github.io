name: Deploy website to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - site.zip
      - site-v2.zip
      - site-v3.zip
      - .github/workflows/deploy-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out deployment bundle
        uses: actions/checkout@v6
      - name: Extract static site
        run: |
          mkdir _site
          unzip -q site.zip -d _site
      - name: Add V2 without changing the original site
        run: |
          mkdir -p _site/v2
          unzip -q site-v2.zip -d _site/v2
      - name: Add V3 without changing the original site or V2
        run: |
          mkdir -p _site/v3
          unzip -q site-v3.zip -d _site/v3
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
